import React from 'react';

const HelloWorldApp = () => {
  return (
    <div>
      <h3 className="text-xl font-semibold mb-2">Welcome to your Web OS!</h3>
      <p>This is a simple "Hello World" application running in a window.</p>
      <p className="mt-4">You can drag this window, resize it, minimize, maximize, and close it.</p>
    </div>
  );
};

export default HelloWorldApp;